# HelloWorld
Trial Edit: Ok At some point I learned structured programming.
When it got to be real work we had change control which was a lot like this
The administrator created the branches and we used that to access current versions of the
code during development and test which was conducted in dedicated(safe) environments
After testing,the new code would be used to generate the next system release.
